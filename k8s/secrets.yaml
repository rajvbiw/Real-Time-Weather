apiVersion: v1
kind: Secret
metadata:
  name: weather-secrets
  namespace: weather-dashboard
type: Opaque
stringData:
  OPENWEATHERMAP_API_KEY: "your_api_key_here" # Replace with actual API key before applying
